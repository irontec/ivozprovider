.. _portals:

################
Platform Portals
################

This section allows configuration of platform portals that will be used by :ref:`Main operators`.

.. warning::
    - URLs MUST be HTTPS
    - URLs MUST not end with slash /

Each URL can also configure a logo and a theme per URL.
