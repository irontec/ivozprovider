;; Outgoing context for terminals calls.
;; ${EXTEN} MUST match a predefined ivozprovider locution
[outgoing]
exten => _[a-zA-Z].,1,NoOp(Playing requested sound ${EXTEN})
    same => n,Progress
    same => n,Wait(1)
    same => n,Playback(ivozprovider/${EXTEN},noanswer)
    same => n,Hangup

