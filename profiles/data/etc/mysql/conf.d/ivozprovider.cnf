[client]
default-character-set = utf8

[mysqld]
default-time-zone = utc
character-set-server = utf8
skip-character-set-client-handshake
default-authentication-plugin=mysql_native_password

# Increase connection max idle timeout
wait_timeout = 604800
