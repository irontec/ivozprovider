#!/bin/bash
# No CLI feeder for webhooks dispatcher.
# The dispatcher subscribes to Redis and processes events automatically.
echo "No feed script available for webhooks. Check Redis channel for events."
