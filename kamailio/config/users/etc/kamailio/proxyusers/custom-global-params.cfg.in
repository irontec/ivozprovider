# This files contains global configuration parameters that may change between environments

# -- Listening interfaces (multiple definitions are allowed)

listen=SET_YOUR_IP_HERE:5060

# -- Debug level

debug=2
memdbg=4
memlog=5
