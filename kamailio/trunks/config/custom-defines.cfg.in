# This files contains 'defines' that may change between environments

# -- Disable logging of dialog initiated by certain SIP methods

##!define NOLOG_INVITE
##!define NOLOG_REGISTER
##!define NOLOG_PUBLISH
##!define NOLOG_SUBSCRIBE
##!define NOLOG_NOTIFY
##!define NOLOG_OPTIONS
##!define NOLOG_MESSAGE
##!define NOLOG_REFER

# -- Enable logging full SIP messages (received and sent)
##!define WITH_PRINTMSG
#!define PRINTMSG_METHODS "INVITE|REGISTER"

# If set to 1 (yes, on), onsend_route block is executed for received replies that are sent out
onsend_route_reply=no

# Debug XMLRPC
#!define DEBUG_XMLRPC

# -- This define will be included as Contact domain in uacrec sent REGISTERs
#!define REG_CONTACT_ADDR "XXX.XXX.XXX.XXX:5060"

# -- XMLRCP allowed IP
#!define XMLRPC_ALLOWED_IP "XXX.XXX.XXX.XXX"

# -- RTPTIMEOUT allowed IP
#!define RTP_TIMEOUT_ALLOWED_IP "XXX.XXX.XXX.XXX"
