#!/bin/bash

set -e

pushd /opt/irontec/ivozprovider/schema

    if [ "$1" != "--skip-db" ]; then
        # Prepare dataset
        bin/prepare-test-db
    fi

    # Run tests
    vendor/bin/simple-phpunit
popd
