#!/bin/bash

set -e

pushd /opt/irontec/ivozprovider/schema

    if [ "$1" != "--skip-db" ]; then
        # Prepare dataset
        bin/prepare-test-db
    fi

    # Run tests
    SYMFONY_PHPUNIT_VERSION=8.5.8 vendor/bin/simple-phpunit
popd
